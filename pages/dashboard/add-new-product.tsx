import Layout from "@/components/layout";
import type { FC } from "react";

interface addNewProductprops {}

const addNewProduct: FC<addNewProductprops> = ({}) => {
  return <Layout>Add new Products</Layout>;
};
export default addNewProduct;
