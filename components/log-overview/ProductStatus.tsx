import React from "react";

interface ProductStatusProps {
  slug: string;
}

function ProductStatus({ slug }: ProductStatusProps) {
  return <div>ProductStatus</div>;
}

export default ProductStatus;
